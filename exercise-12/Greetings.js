"use strict";
let friends = ["Usman", "Shehroz", "Asim", "Babar"];
let message = "you are my best friend";
for (let i = 0; i < friends.length; i++) {
    const element = friends[i];
    console.log(`${element} ${message} `);
}
