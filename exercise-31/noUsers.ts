let friends:string[] = ["Usman","Shehroz","Asim","Babar"];

// original list
if (friends.length > 0) {
    console.log("original list of friends:",friends);
} 

// remove all the friends from array
friends = [];

if (friends.length === 0) {
    console.log("we need to find some friends");  
} else {
    console.log("original list of friends:",friends);
}