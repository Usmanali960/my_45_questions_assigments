"use strict";
let favoriteTransfortation = ["motorCycle", "car", "aeroplane", "train"];
let message = "I would like to own a";
favoriteTransfortation.forEach(elem => {
    console.log(`${message} ${elem}`);
});
